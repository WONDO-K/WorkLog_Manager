# Codex 연동 안내

이 앱은 대화 내용을 직접 읽지 않습니다. Codex 같은 로컬 에이전트가 사용자의 작업 신호를 해석해 HTTP API로 구조화된 기록을 저장합니다. 서버가 실행 중이어야 하며 기본 주소는 `http://127.0.0.1:8765`입니다.

## 태스크 기록

- `GET /api/tasks`: 현재 태스크 목록 확인
- `GET /api/tasks/{id}`: 태스크와 작업 이력 확인
- `POST /api/tasks`: 새 요청 생성
- `PATCH /api/tasks/{id}`: 최신 `version`과 함께 정보·상태·진행 기록 갱신

새 태스크 본문:

```json
{"title":"요청 제목","request":"요청 배경과 내용","criteria":"완료 기준","deadline":"","target_date":"","next_action":"다음 행동"}
```

진행 이력은 기존 태스크 응답의 필드를 유지하고 `version`, `status`, `note`를 함께 전송합니다. `완료` 변경에는 검증 근거, `보류` 변경에는 사유가 필요합니다. `409` 응답이면 최신 기록을 다시 읽고 사용자의 입력과 충돌을 조정합니다.

## 일일 마감

- `GET /api/daily-logs/YYYY-MM-DD`: 저장된 일일 기록 또는 빈 초안 확인
- `PUT /api/daily-logs/YYYY-MM-DD`: 내부용·외부용 초안 저장
- `POST /api/daily-logs/YYYY-MM-DD/confirm`: 사용자가 검토한 외부용 기록 확정

초안 저장 본문:

```json
{"version":0,"internal_md":"# 내부용 요약","public_md":"# 외부용 초안"}
```

확정 본문은 `{"version":1}` 형식입니다. 확정은 반드시 사용자의 공개 범위 검토 후 수행합니다. `exportDir`이 설정되어 있으면 확정 시 `YYYY-MM-DD.internal.md`와 `YYYY-MM-DD.md`를 생성하며 응답의 `exported`가 `true`가 됩니다. 확정 뒤 내용을 수정하면 상태는 다시 `초안`이 됩니다.

일일 요약은 요청 → 분석 → 판단 → 수정 → 검증 → 성과 흐름과 다음 10개 항목을 사용합니다.

1. 핵심 성과 요약
2. 질문/문제 인식
3. 문제 해결 과정
4. 조치/판단 내용
5. 배운 개념/기술 학습 포인트
6. 개선 효과
7. 성능/효율 영향
8. 수치화 후보
9. 이력서/성과 문장 후보
10. 추가 확인 질문

내부용에는 구체적 근거를 남기고, 외부용에는 내부 식별 정보와 공개 여부가 불명확한 데이터를 제거하거나 일반화합니다. 확인되지 않은 수행·성과·수치는 만들지 않습니다.
